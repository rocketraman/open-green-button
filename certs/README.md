Public certificate information for Open Green Button client certificates.

* `ogb-ca.crt`: The self-signed Open Green Button CA cert.
* `ogb-client.crt`: The currently active leaf certificate, signed by `ogb-ca.crt`.
* `ogb-client-bundle.crt`: One bundle containing both of the above.
