# Per-utility profiles. Endpoint URLs, scopes, and paths live in this file; the clientId and
# clientSecret are treated as secrets and come from env vars (`fly secrets set` in prod,
# `mise.local.toml` for local dev) keyed on the utility id.
#
# To add a utility:
#   1. Add a new entry in the `utilities` array, in alphabetical order (for the purposes of
#      ordering, treat commented blocks the same as uncommented ones).
#   2. Set the OPENGB_UTILITY_<ID>_CLIENTID and OPENGB_UTILITY_<ID>_CLIENTSECRET env vars.
#   3. Restart the server. (Or for Fly, `fly deploy`.)
#
# The display name format is "<Utility Name>, <Location e.g. state or province>"
#
# defaultScope format reference: https://github.com/green-button/green-button.github.io/blob/master/espi/authorization_scope.md
#
# Our scope can request both auth styles (FB_14 legacy, FB_31 modern) when utilities differ
# on which side of the upgrade they're on. The utility grants whichever it supports; the
# *granted* scope (which may be a subset) comes back in the token response and is what we
# persist. See RefreshBlob construction in routes/Connect.kt for the dual-style handling.
#
# Format params we ideally tack onto every scope string:
#   IntervalDuration  900_3600  accept both 15-min and 1-hr readings; utility serves whichever
#   BlockDuration     Monthly   billing-aligned aggregates
#   HistoryLength     94608000  36 months — asks the *utility* how much history to authorize
#   SubscriptionFrequency  Daily  daily push notifications
#   AccountCollection 5         covers elec + gas + water + spare
#
# published-min/max wire format: the ESPI resource GET sends these date-range query params as ISO
# 8601 (e.g. `2026-06-08T00:00:00Z`). A `published-max` in the future is clamped to now before the
# request — no custodian publishes future-dated readings, and some (savagedata) reject a future
# published-max with a bare 400. See proxy/UsageClient.kt.
#
# TLS client authentication (mTLS): the default cert lives in application.conf's `clientAuth` block
# and is used for every utility here. A utility with different requirements (e.g. a cert the utility
# issues rather than our self-signed default) can override it with its own `clientAuth` block —
# same shape, backed by its own env/Fly secret — which takes precedence over the default. See the
# PG&E template below for the syntax.
#
# `initialHistory` (per-utility, below) is a separate, client-facing knob: how far back the Home
# Assistant client backfills `published-min` on the *first* fetch. Expressed human-friendly —
# `2y`, `6m`, `90d` (units: d/w/m/y; m=30d, y=365d). It's surfaced to the client in the claim
# response (as seconds) and is the single source of truth for the backfill window. The scope
# `HistoryLength` above is the utility-facing analogue; the two normally agree, but for the
# test-lab harness the scope value isn't accepted, so `initialHistory` is the effective control.

utilities = [
  # ---------------------------------------------------------------------------------------------
  # Alectra
  #
  # Operated by Savage Data — production Data Custodian (promoted from the certification sandbox
  # 2026-07-22).
  #
  # Same ESPI 3.3 dynamic-registration flow as Milton Hydro, but a DISTINCT app registration —
  # own clientId/secret, own scope (note: no "35" in the FB list here, unlike Milton's).
  #
  # Registered redirect_uri: https://api.opengreenbutton.org/connect/alectra/callback
  # Registered thirdPartyNotifyUri: https://api.opengreenbutton.org/notify/alectra
  {
    id = "alectra"
    displayName = "Alectra, Ontario"
    authorizeUrl = "https://alectradc.savagedata.com/connect/authorize"
    tokenUrl = "https://alectradc.savagedata.com/connect/token"
    clientId = ${?OPENGB_UTILITY_ALECTRA_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_ALECTRA_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_6_7_8_9_12_15_16_17_27_28_36_37_40_51_54_55_56_57_58_59_60_61;SubscriptionFrequency=Daily"
  }

  {
    # Burlington Hydro
    #
    # Operated by London Hydro
    #
    id = "burlington_hydro"
    displayName = "Burlington Hydro, Ontario"
    authorizeUrl = "https://greenbutton.burlingtonhydro.com/oauth/authorize"
    tokenUrl = "https://greenbutton.burlingtonhydro.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_BURLINGTON_HYDRO_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_BURLINGTON_HYDRO_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_68_69"
    # refreshScope defaults to `Granted` (see utility/RefreshScope.kt). Burlington echoes this full
    # scope at exchange and accepts it verbatim on refresh — AND accepts a scopeless refresh —
    # confirmed 2026-07-10 via onboardProbeBurlingtonRefreshScope, so the Granted default is safe.
    # NOTE: do NOT set quirks.dateFilterParam = "updated" here. That was tried 2026-07-08 on
    # the theory that published-min never returns fresh data — it was reverted once a direct
    # probe (bypassing a since-fixed HA-side cursor bug) showed updated-min returns the FULL
    # ~2.5-year history regardless of window (narrow and wide requests returned an identical
    # 22,008 readings), which is not incremental at all and would violate the incremental-only
    # commitment made to Burlington. A correctly-scoped published-min probe, once run with a
    # healthy (non-poisoned) cursor, returned exactly the newest day of data as expected — see
    # docs/utilities/burlington-incremental-issue.md for the full investigation.
  }

  # Consumers Energy
  #
  # Operated by Utility API
  #
  {
    id = "consumers_energy"
    displayName = "Consumers Energy, Michigan"
    authorizeUrl = "https://greenbutton.consumersenergy.com/oauth/authorize"
    tokenUrl = "https://greenbutton.consumersenergy.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_CONSUMERS_ENERGY_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_CONSUMERS_ENERGY_CLIENTSECRET}
    defaultScope = "FB=4_16_51;AdditionalScope=historical-2y_ongoing-1y_meters-all_sou-default"
  }

  # ---------------------------------------------------------------------------------------------
  # Elexicon Energy
  #
  # Operated by Savage Data — production Data Custodian (promoted 2026-07-22).
  #
  {
    id = "elexicon_energy"
    displayName = "Elexicon Energy, Ontario"
    authorizeUrl = "https://elexicondc.savagedata.com/connect/authorize"
    tokenUrl = "https://elexicondc.savagedata.com/connect/token"
    clientId = ${?OPENGB_UTILITY_ELEXICON_ENERGY_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_ELEXICON_ENERGY_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_6_8_12_15_16_17_27_28_36_37_40_51_54_55_56_57_58_59_60_61;SubscriptionFrequency=Daily"
  }

  # ---------------------------------------------------------------------------------------------
  # Elk Energy
  #
  # Operated by London Hydro
  #
  {
    id = "elk_energy"
    displayName = "Elk Energy, Ontario"
    authorizeUrl = "https://greenbutton.elkenergy.com/oauth/authorize"
    tokenUrl = "https://greenbutton.elkenergy.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_ELK_ENERGY_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_ELK_ENERGY_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # ---------------------------------------------------------------------------------------------
  # Enwin
  #
  # Operated by London Hydro
  #
  {
    id = "enwin"
    displayName = "Enwin, Ontario"
    authorizeUrl = "https://greenbutton.enwin.com/oauth/authorize"
    tokenUrl = "https://greenbutton.enwin.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_ENWIN_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_ENWIN_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # ---------------------------------------------------------------------------------------------
  # Festival Hydro
  #
  # Operated by London Hydro
  #
  {
    id = "festival_hydro"
    displayName = "Festival Hydro, Ontario"
    authorizeUrl = "https://greenbutton.festivalhydro.com/oauth/authorize"
    tokenUrl = "https://greenbutton.festivalhydro.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_FESTIVAL_HYDRO_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_FESTIVAL_HYDRO_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # ---------------------------------------------------------------------------------------------
  # Hydro Ottawa
  #
  # Operated by Savage Data — production Data Custodian (promoted 2026-07-22).
  #
  {
    id = "hydro_ottawa"
    displayName = "Hydro Ottawa, Ontario"
    authorizeUrl = "https://hydroottawadc.savagedata.com/connect/authorize"
    tokenUrl = "https://hydroottawadc.savagedata.com/connect/token"
    clientId = ${?OPENGB_UTILITY_HYDRO_OTTAWA_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_HYDRO_OTTAWA_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_6_8_12_15_16_17_27_28_36_37_40_51_54_55_56_57_58_59_60_61;SubscriptionFrequency=Daily"
  }

  # ---------------------------------------------------------------------------------------------
  # Kentucky Utilities (KU) — part of LG&E and KU Energy (PPL)
  #
  # Scope history (empirically bracketed) — authorize and refresh scopes are DECOUPLED
  # (defaultScope = /authorize, refreshScope = refresh grant; see utility/RefreshScope.kt):
  #   - /authorize: only the FULL scope below works. No-scope and the bare FB list both fail with
  #     `invalid_scope` at /authorize (confirmed 2026-07-10). So KU REQUIRES the full param string
  #     to start the flow; defaultScope stays as-is.
  #   - refresh: refreshScope defaults to `Granted` and is required for KU — a SCOPELESS refresh
  #     should use the originally granted scope, but KU fails it with `invalid_scope` "requested
  #     does not match the scope granted by the resource owner" (commit 3051c70);
  #
  {
    id = "kentucky_utilities"
    displayName = "Kentucky Utilities, Kentucky"
    authorizeUrl = "https://mymeter.lge-ku.com/OAuthServer/authorize"
    tokenUrl = "https://mymeter.lge-ku.com/OAuthServer/token"
    clientId = ${?OPENGB_UTILITY_KENTUCKY_UTILITIES_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_KENTUCKY_UTILITIES_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_15_16_32_37_39;IntervalDuration=900_3600;BlockDuration=Monthly;SubscriptionFrequency=Daily;HistoryLength=94608000"
  }

  # ---------------------------------------------------------------------------------------------
  # London Hydro (production)
  #
  # Registered as a Green Button third-party consumer with London Hydro's production portal.
  # Endpoints/credentials supplied by London Hydro (2026-07-21). On the London Hydro registration
  # they have:
  #   redirect_uri        = https://<publicBaseUrl>/connect/london_hydro/callback
  #   thirdPartyNotifyUri = https://<publicBaseUrl>/notify/london_hydro
  # defaultScope is the bare FB list London Hydro issued — the extended IntervalDuration/
  # HistoryLength/etc. params were rejected with `invalid_scope`, so keep it trimmed.
  #
  {
    id = "london_hydro"
    displayName = "London Hydro, Ontario"
    authorizeUrl = "https://greenbutton.londonhydro.com/oauth/authorize"
    tokenUrl = "https://greenbutton.londonhydro.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_LONDON_HYDRO_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_LONDON_HYDRO_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_68_69"
  }

  # ---------------------------------------------------------------------------------------------
  # Milton Hydro
  #
  # Operated by Savage Data — production Data Custodian (promoted 2026-07-22).
  #
  {
    id = "milton_hydro"
    displayName = "Milton Hydro, Ontario"
    authorizeUrl = "https://miltondc.savagedata.com/connect/authorize"
    tokenUrl = "https://miltondc.savagedata.com/connect/token"
    clientId = ${?OPENGB_UTILITY_MILTON_HYDRO_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_MILTON_HYDRO_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_6_7_8_9_12_15_16_17_27_28_35_36_37_40_51_54_55_56_57_58_59_60_61;SubscriptionFrequency=Daily"
  }

  # ---------------------------------------------------------------------------------------------
  # Newmarket-Tay (NT) Power
  #
  # Operated by London Hydro
  #
  {
    id = "nt_power"
    displayName = "Newmarket-Tay (NT) Power, Ontario"
    authorizeUrl = "https://greenbutton.ntpower.ca/oauth/authorize"
    tokenUrl = "https://greenbutton.ntpower.ca/oauth/token"
    clientId = ${?OPENGB_UTILITY_NT_POWER_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_NT_POWER_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # ---------------------------------------------------------------------------------------------
  # Niagara Peninsula Energy (NPEI)
  #
  # Operated by London Hydro
  #
  {
    id = "npe"
    displayName = "Niagara Peninsula Energy, Ontario"
    authorizeUrl = "https://greenbutton.npei.ca/oauth/authorize"
    tokenUrl = "https://greenbutton.npei.ca/oauth/token"
    clientId = ${?OPENGB_UTILITY_NPE_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_NPE_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # Oakville Hydro
  #
  # Operated by London Hydro
  #
  {
    id = "oakville_hydro"
    displayName = "Oakville Hydro, Ontario"
    authorizeUrl = "https://greenbutton.oakvillehydro.com/oauth/authorize"
    tokenUrl = "https://greenbutton.oakvillehydro.com/oauth/token"
    clientId = ${?OPENGB_UTILITY_OAKVILLE_HYDRO_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_OAKVILLE_HYDRO_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # Oshawa Power (OPUC)
  #
  # Operated by London Hydro
  #
  {
    id = "oshawa_power"
    displayName = "Oshawa Power, Ontario"
    authorizeUrl = "https://greenbutton.oshawapower.ca/oauth/authorize"
    tokenUrl = "https://greenbutton.oshawapower.ca/oauth/token"
    clientId = ${?OPENGB_UTILITY_OSHAWA_POWER_CLIENTID}
    clientSecret = ${?OPENGB_UTILITY_OSHAWA_POWER_CLIENTSECRET}
    defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_69"
  }

  # ---------------------------------------------------------------------------------------------
  # PG&E (Pacific Gas & Electric) — Share My Data. NOT YET LIVE.
  #
  # PG&E's Third Party registration collects a `thirdPartyScopeSelectionScreenURI` (the "Third
  # Party Scope Selection URI"): the screen a custodian sends the customer to, during a
  # custodian-initiated OAuth flow, to confirm what they'll share. We serve that at
  #   https://<publicBaseUrl>/connect/pge/scope         (handled by routes/Connect.kt)
  # Also give PG&E on the same form:
  #   redirect_uri  = https://<publicBaseUrl>/connect/pge/callback
  #   thirdPartyNotifyUri = https://<publicBaseUrl>/notify/pge
  #
  # To activate: fill in PG&E's sandbox (then production) authorize/token URLs and the client_id
  # they issue, set OPENGB_UTILITY_PGE_CLIENTSECRET (Fly secret / mise.local.toml), uncomment, and deploy.
  # Start against PG&E's sandbox; flip the URLs to production only after certification. Confirm
  # PG&E accepts this defaultScope (their sandbox may reject the extended format params, exactly
  # like the Burlington/London harnesses did — trim to the bare FB list if so).
  #
  # {
  #   id = "pge"
  #   displayName = "PG&E"
  #   authorizeUrl = "https://api.pge.com/.../oauth/authorize"   # TODO: real PG&E sandbox URL
  #   tokenUrl     = "https://api.pge.com/.../oauth/token"       # TODO: real PG&E sandbox URL
  #   clientId = "TODO_PGE_CLIENT_ID"
  #   clientSecret = ${?OPENGB_UTILITY_PGE_CLIENTSECRET}
  #   defaultScope = "FB=1_3_4_5_13_15_16_28_31_37_39_51_53_54_55_56_57_58_59_60_61_64_65_68_69"
  #
  #   # Optional per-utility mTLS override — only if PG&E requires a different client cert than our
  #   # default self-signed one. Omit this block to use application.conf's default `clientAuth`.
  #   # clientAuth {
  #   #   keystoreBase64 = ${?OPENGB_UTILITY_PGE_CLIENTAUTH_KEYSTOREBASE64}
  #   #   keystorePassword = ${?OPENGB_UTILITY_PGE_CLIENTAUTH_KEYSTOREPASSWORD}
  #   # }
  # }

]
